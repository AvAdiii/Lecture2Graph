# Multi-modal ingest (M1–M2)
yt-dlp
openai-whisper          # ASR (code-mixed Hindi/English)
pytesseract             # OCR over handwritten lecture boards
Pillow                  # OCR pre-processing

# Extraction, fusion, evaluation
rapidfuzz               # ASR-seeded fuzzy OCR correction + concept resolution
networkx                # DAG construction, cycle-breaking, topological sort

# Neural extractor: talks to a LOCAL OpenAI-compatible server (Ollama by
# default) over stdlib urllib, no Python SDK needed. See lecture2graph/neural/llm.py.
# Install + run a model first, e.g.:  ollama pull llama3.1:8b && ollama serve
